filters:
  and:
    - file.folder == "docs/tasks/issues"
views:
  - type: table
    name: All tasks
    columns: [title, status, area, priority, owner, updated]
    sort:
      - column: updated
        direction: desc
