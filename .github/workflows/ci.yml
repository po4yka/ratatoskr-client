name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

# Cancel previous runs for the same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Lint check
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          gradle-home-cache-cleanup: true

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run ktlint
        run: ./gradlew ktlintCheck --no-daemon

      - name: Upload lint results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: lint-results
          path: |
            **/build/reports/ktlint/
          retention-days: 7

  # Build and test all platforms
  build-all:
    name: Build All Platforms
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: android
            java-version: '17'
          - os: macos-14
            platform: ios
            java-version: '17'

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0  # Full history for better caching

      - name: Set up JDK ${{ matrix.java-version }}
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: ${{ matrix.java-version }}

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          gradle-home-cache-cleanup: true
          cache-encryption-key: ${{ secrets.GRADLE_CACHE_ENCRYPTION_KEY }}

      - name: Cache Konan dependencies (iOS only)
        if: matrix.platform == 'ios'
        uses: actions/cache@v5
        with:
          path: ~/.konan
          key: ${{ runner.os }}-konan-${{ hashFiles('**/*.gradle.kts', 'gradle/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-konan-

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build active Kotlin modules
        run: ./gradlew :core:common:build :core:data:build :core:navigation:build :core:ui:build :feature:auth:build :feature:collections:build :feature:digest:build :feature:settings:build :feature:summary:build :feature:sync:build --no-daemon

      - name: Run Kotlin module tests
        run: ./gradlew :core:common:allTests :core:data:allTests :core:navigation:allTests :core:ui:allTests :feature:auth:allTests :feature:collections:allTests :feature:digest:allTests :feature:settings:allTests :feature:summary:allTests :feature:sync:allTests --no-daemon

      # Android-specific tasks
      - name: Build Android Debug APK
        if: matrix.platform == 'android'
        run: ./gradlew :androidApp:assembleDebug --no-daemon

      - name: Build Android Release APK
        if: matrix.platform == 'android'
        run: ./gradlew :androidApp:assembleRelease --no-daemon
        continue-on-error: true  # May fail without signing keys

      - name: Run Android tests
        if: matrix.platform == 'android'
        run: ./gradlew :composeApp:testAndroidHostTest --no-daemon

      # iOS-specific tasks
      - name: Build iOS frameworks
        if: matrix.platform == 'ios'
        run: |
          ./gradlew :composeApp:linkPodDebugFrameworkIosSimulatorArm64 --no-daemon
          ./gradlew :composeApp:linkPodDebugFrameworkIosArm64 --no-daemon

      - name: Run iOS tests
        if: matrix.platform == 'ios'
        run: ./gradlew :core:common:iosSimulatorArm64Test :core:data:iosSimulatorArm64Test :feature:auth:iosSimulatorArm64Test :feature:collections:iosSimulatorArm64Test :feature:digest:iosSimulatorArm64Test :feature:settings:iosSimulatorArm64Test :feature:summary:iosSimulatorArm64Test :feature:sync:iosSimulatorArm64Test --no-daemon

      # Upload artifacts
      - name: Upload Android Debug APK
        if: matrix.platform == 'android'
        uses: actions/upload-artifact@v7
        with:
          name: android-debug-apk-${{ github.sha }}
          path: androidApp/build/outputs/apk/debug/*.apk
          retention-days: 14

      - name: Upload Android Release APK
        if: matrix.platform == 'android' && hashFiles('androidApp/build/outputs/apk/release/*.apk') != ''
        uses: actions/upload-artifact@v7
        with:
          name: android-release-apk-${{ github.sha }}
          path: androidApp/build/outputs/apk/release/*.apk
          retention-days: 14

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results-${{ matrix.platform }}-${{ github.sha }}
          path: |
            **/build/reports/tests/
          retention-days: 14

  # Generate test coverage report
  coverage:
    name: Test Coverage
    runs-on: ubuntu-latest
    needs: build-all

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Generate coverage report
        run: ./gradlew :core:common:koverHtmlReportDebug :core:data:koverHtmlReportDebug --no-daemon || ./gradlew jacocoTestReport --no-daemon || echo "Coverage not configured, skipping"
        continue-on-error: true

      - name: Upload coverage reports
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: coverage-reports-${{ github.sha }}
          path: |
            core/common/build/reports/kover/
            core/data/build/reports/kover/
            composeApp/build/reports/jacoco/
          retention-days: 30
        continue-on-error: true

      - name: Comment coverage on PR
        if: github.event_name == 'pull_request' && hashFiles('core/common/build/reports/kover/htmlDebug/index.html') != ''
        uses: madrapps/jacoco-report@v1.7.2
        with:
          paths: core/common/build/reports/kover/htmlDebug/index.html
          token: ${{ secrets.GITHUB_TOKEN }}
          min-coverage-overall: 75
          min-coverage-changed-files: 80

  # Dependency vulnerability check
  security-check:
    name: Security & Dependency Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Dependency vulnerability check
        run: ./gradlew dependencyCheckAnalyze --no-daemon || echo "OWASP dependency check not configured"
        continue-on-error: true

      - name: Check for dependency updates
        run: ./gradlew dependencyUpdates --no-daemon || echo "Dependency updates check not available"
        continue-on-error: true

      - name: Upload security report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: security-report
          path: build/reports/
          retention-days: 7
        continue-on-error: true

  # Status check for all jobs
  status-check:
    name: Status Check
    runs-on: ubuntu-latest
    needs: [lint, build-all, coverage, security-check]
    if: always()

    steps:
      - name: Check build status
        run: |
          if [ "${{ needs.lint.result }}" != "success" ] || \
             [ "${{ needs.build-all.result }}" != "success" ]; then
            echo "One or more critical jobs failed"
            exit 1
          fi
          echo "All critical checks passed!"

  # Notification on failure
  notify-failure:
    name: Notify on Failure
    runs-on: ubuntu-latest
    needs: [ build-all, coverage, security-check ]
    if: failure()

    steps:
      - name: Build failed notification
        run: |
          echo "::error::CI build failed for commit ${{ github.sha }} on branch ${{ github.ref_name }}"
          echo "Check the workflow run at: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
