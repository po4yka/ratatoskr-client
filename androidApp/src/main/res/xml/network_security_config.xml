<?xml version="1.0" encoding="utf-8"?>
<!--
  Restricts the trust anchor set to system CAs only. Without this config Android
  7+ also trusts user-installed CAs, which means a single sideloaded root on the
  device can MITM all of our traffic before certificate pinning lands.

  Debug overrides re-allow user CAs so emulator HTTPS-proxy workflows (Charles,
  mitmproxy, etc.) still work. Release builds never honour the overrides.

  TODO: once the certificate-pinning task lands, attach a <pin-set> with at
  least two pins to the ratatoskr-api.po4yka.com <domain-config>. Until then
  pinning is intentionally absent — we keep this config focused on the trust
  anchor restriction.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
