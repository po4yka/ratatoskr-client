package com.po4yka.ratatoskr.data.remote

/**
 * Placeholder until the generated-API test fixtures land.
 *
 * TODO(api-migration): the hand-written `KtorSyncApi` was deleted as part
 * of the migration to `com.po4yka.ratatoskr.api.generated.api.SyncApi`.
 * Equivalent coverage needs to mock `Api.client` (the bootstrapped
 * generated client) instead of constructing a custom `HttpClient`. The
 * private declaration below carries the file past the `no-empty-file`
 * ktlint rule without introducing a runnable empty test.
 */
private object KtorSyncApiTestPlaceholder
