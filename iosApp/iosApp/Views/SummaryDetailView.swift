import SwiftUI
import Shared

struct SummaryDetailView: View {
    // TODO: Integrate with SummaryDetailViewModel
    
    var body: some View {
        ScrollView {
            Text("Summary Content")
        }
        .navigationTitle("Detail")
    }
}